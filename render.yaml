version: "1"

services:
  - type: web
    name: listena
    env: node
    region: oregon
    plan: free
    buildCommand: npm ci && npx prisma generate && npx prisma migrate deploy && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: listena-db
          property: connectionString
      - key: NEXTAUTH_URL
        value: https://listena.onrender.com
      - key: NEXTAUTH_SECRET
        generateValue: true
      # AI is intentionally unconfigured until OPENAI_API_KEY is added through
      # the host secret manager with AI_PROVIDER=openai. The application then
      # fails closed instead of presenting a mock response.
      - key: TTS_PROVIDER
        value: mock
      - key: LOG_LEVEL
        value: info

databases:
  - name: listena-db
    databaseName: listena
    user: listena
    region: oregon
    plan: free
