# ListenAI kernel — 0.5.0
# CURRENT CHECKPOINT 2026-09-10: Plan06 is deployed to https://listena-english.tuanank2112.workers.dev (Worker `8d50494f-773f-46bb-9910-23c4474d9b4d`). Production D1 received additive migrations `0002`/`0003` and an idempotent core-curriculum import: 5 lessons, 116 vocabulary items, 20 segments and 54 exercises; the existing user was preserved. Local gates: 204 unit tests, type-check, lint 0 errors/36 existing warnings, Worker build and E2E 16/16. The live-AI production gate is deliberately OPEN: `OPENAI_API_KEY` has not yet been installed as a Worker secret, so calls fail honestly rather than mock. Read planning/06_2026-09-10_personalized-ai-learning/plan.md.
<agent_startup_protocol>
0. Skill .xay-dung-nao-bo từ Fitc84/brain4agent.old: init_brain.js <repo> --check. Xem binding đường dẫn trong AGENTS.md; chỉ ghi khi cần nâng cấp.
1. Đọc kernel → index.md → memory/hot/today.md và state.json → plan và docs module.
2. Đối chiếu mã thật trước khi tin báo cáo lịch sử. Đọc docs Next cục bộ trước sửa framework.
3. Không reset/seed DB người dùng; E2E dùng database tạm riêng và test-process-only Responses transport stub. Production runtime không có đường chọn mock AI.
4. Kết thúc: đồng bộ plan, docs, roadmap, changelog và hot memory theo .compact.
</agent_startup_protocol>

## Sản phẩm
- User xác nhận 2026-09-07: TỰ HỌC TIẾNG ANH AI-NATIVE; AI dẫn dắt trải nghiệm.
- Loop: mục tiêu → nhiệm vụ/ngữ cảnh → learner response → coaching → comeback → evidence → bước tiếp theo thích ứng.
- Mission, Lesson Coach, Daily Quest là entry chính; lesson/quiz/dictation/flashcards hỗ trợ sửa lỗi.
- Không coi chatbot phụ hay course-first là định hướng sản phẩm. Không suy hiệu quả học tập từ tests xanh.

## Kiến trúc
- Next16.3.3 App Router, React19.2.8, TypeScript, Tailwind4, Auth.js5 Credentials/JWT, OpenNext Cloudflare adapter.
- Prisma6 with libSQL local SQLite and request-scoped D1 in Cloudflare production; D1 schema uses additive `migrations/0001`–`0003`. Core curriculum is restored by the reviewed idempotent builder, never reset/seed.
- Session: server/learning → ai/tutor-orchestrator → live Responses provider or typed unavailable error → transaction → DTO.
- State, validator, evidence/mastery thuộc server; không đưa acceptedAnswers/correctIndex xuống client.
- clientTurnId chống ghi trùng; auto/manual completion tăng thời gian một lần.
- English WebSpeech; Vietnamese VieNeu + WebSpeech fallback. Kokoro còn legacy, không đăng ký runtime.
- Dataset in production: 5 tracked lessons, 116 vocabulary, 10 grammar topics, 54 exercises and 20 segments. Educaplay is reference only.
- Educaplay chỉ tham khảo, không đưa vào curriculum/audio.

## Công việc
- 2026-09-10: Plan04 closed local. Daily Quest now consumes bounded owned scenario history; completion needs server-owned evidence, distinguishes PARTIAL from COMPLETED; dashboard/progress adaptive meters resolve SkillMastery first. Keep legacy attempts/reviews/games outside unified evidence until a new source contract exists.
- 2026-09-08 20:30: Plan03 closed local. Root stopped stale repo Next dev server PID 24896; E2E then launched normally. Auto-completion CTA can be PRACTICE by design when mastery <0.6; deterministic mock now honors successful BOSS shouldComplete/DEBRIEF and has regression test.
- 2026-09-07 20:55: P13 eval không còn self-assert; run.ts gọi startMission/evaluateTutorTurn với DeterministicMockTutorProvider, kiểm phase + grounded IDs. Kết quả 15/15, grounded 4/15 đúng kỳ vọng.
- Current plan: planning/06_2026-09-10_personalized-ai-learning/plan.md, target0.5.0 MINOR. Private AI lessons, server-owned adaptive games and additive curriculum recovery are deployed; the only live-AI exit gate is the user-owned Worker secret plus a real smoke.
- User cho phép orchestrator điều phối model: Astra runtime, Sol UI, Luna utility; root contracts/integration.
- Subagents bị usage limit giữa chừng; root tiếp quản, không coi code dở là đã nghiệm thu.
- Baseline current: 204 unit tests; kết quả cuối đọc state.json và Plan06 TESTING-ACCEPTANCE.
- Roadmap: live-provider smoke/quotas, pedagogical evaluation with learners, learner memory xuyên phiên, next action có evidence and optional audio coverage.
